# Docker Compose version
version: '3.8'

services:
  # Main application database
  # This is the primary PostgreSQL database for the CRM Object Sync application
  db:
    image: postgres:15
    ports:
      - "5433:5432"  # Maps container port 5432 to host port 5433 to avoid conflicts
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data  # Persists database data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]  # Checks if database is ready
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - default

  # OAuth service database
  # Separate PostgreSQL database for the OAuth service to store tokens and auth data
  oauth-db:
    image: postgres:15
    env_file:
      - ./oauth-service.env  # Loads environment variables from oauth-service.env
    volumes:
      - oauth_postgres_data:/var/lib/postgresql/data  # Persists OAuth database data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${OAUTH_POSTGRES_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - default

  # Main application's migration service
  # Handles database migrations for the main application using Prisma
  prisma-migrate:
    build:
      context: .
      target: production
    command: npx prisma migrate deploy  # Runs database migrations
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy  # Waits for main database to be healthy
    networks:
      - default

  # OAuth service migration service
  # Handles database migrations for the OAuth service
  oauth-prisma-migrate:
    build:
      context: https://github.com/hubspotdev/oauth-service.git#containerization
      target: production
    command: npx prisma migrate deploy
    env_file:
      - ./oauth-service.env
    depends_on:
      oauth-db:
        condition: service_healthy  # Waits for OAuth database to be healthy
    networks:
      - default

  # Main application service
  # The CRM Object Sync application
  app:
    build:
      context: .
      target: production
    command: npm run start
    ports:
      - "3000:3000"  # Exposes the main application on port 3000
    env_file:
      - .env
    environment:
      NODE_ENV: production
    depends_on:
      db:
        condition: service_healthy  # Waits for main database
      prisma-migrate:
        condition: service_completed_successfully  # Waits for migrations
      oauth-service:
        condition: service_healthy  # Waits for OAuth service
    networks:
      - default
    restart: always  # Automatically restarts if the service crashes
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:3000/api-docs || exit 1"]  # Checks if API is accessible
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s  # Gives the service time to start up

  # OAuth service
  # Handles HubSpot OAuth authentication and token management
  oauth-service:
    build:
      context: https://github.com/hubspotdev/oauth-service.git#containerization
    env_file:
      - ./oauth-service.env
    ports:
      - "3001:3001"  # Exposes the OAuth service on port 3001
    depends_on:
      oauth-db:
        condition: service_healthy  # Waits for OAuth database
      oauth-prisma-migrate:
        condition: service_completed_successfully  # Waits for OAuth migrations
    networks:
      - default
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:3001/api/get-install-url || exit 1"]  # Checks if OAuth service is ready
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    restart: always

# Volume definitions
# These volumes persist data between container restarts
volumes:
  postgres_data:  # For main application database
  oauth_postgres_data:  # For OAuth service database

# Network configuration
# Creates a bridge network for inter-service communication
networks:
  default:
    driver: bridge
